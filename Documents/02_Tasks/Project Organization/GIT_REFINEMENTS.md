## Tasks
- Create a document for GIT BRANCH DIRECTORY which outlines each branch we will have for the game and their use cases.
- Add to the document to make sure we have all nessisary branches to make development optimized!
- DO NOT DELETE ANY BRANCH WITHOUT APPROVAL
- See which branches can be merged, and merge them. If there is a failure, automatically fix the errors then merge again.
- Look over the document again and refine as needed.
- Create the branches from the outline created.
- Create a README.md document somewhere for this so the user can see them on GitHub.
- 